{
  "name": "dino",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prettier": "^1.12.1",
    "rc-slider": "^8.6.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-fullpage": "^0.1.18",
    "react-scripts": "1.1.4",
    "styled-components": "^3.2.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy-now": "yarn build && rm ./build/static/js/*.js.map",
    "deploy-now": "now ./build --public --name=dino"
  }
}
